[project]
name = "pytorch-for-ai"
version = "0.1.0"
description = a " hands-on pytorch (tensors, autograd, nn.Module, data loading, training loops, saving/loading, GPU + multi-GPU training)."
readme = "README.md"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.13.0",
    "torchvision>=0.20.0",
    "numpy>=2.2.6",
    "matplotlib>=3.10.9",
    "pandas>=2.2.0",
    "ipykernel>=7.3.0",
    "jupyter>=1.1.1",
    "nbclient>=0.11.0",
    "nbformat>=5.10.4",
]
